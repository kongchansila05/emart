import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF21586E);
  static const secondary = Color(0xFFDF3232);
  static const circleBackground = Color(0xFFAED6E0);
  static const accent = Color(
    0xFFF0BD4E,
  ); // highlight color used for UI emphasis.

  static const background = Color(0xFFFFFFFF);
  static const surface = Color(
    0xFFE0E1E3,
  ); // Color of UI surfaces like cards, modals, panels, containers.

  static const textPrimary = Color(
    0xFFA9A9A9,
  ); // Used for titles and important content.
  static const textSecondary = Color(
    0xFF6B7280,
  ); // Used for subtitles, descriptions, hints.

  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  static const border = Color(0xFFE5E7EB);
}
