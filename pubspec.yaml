name: mart24
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  dio: ^5.9.0

  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0

  google_sign_in: ^6.3.0

  sign_in_with_apple: ^6.1.4
  
  carousel_slider: ^5.1.2
  enefty_icons: ^1.0.10
  liquid_glass_ui: ^0.4.0
  image_picker: ^1.1.2
  flutter_slidable: ^3.1.2
  shared_preferences: ^2.5.3
  geolocator: ^13.0.2
  url_launcher: ^6.3.1
  intl_phone_field:
    path: packages/intl_phone_field

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
